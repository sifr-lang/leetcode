# LeetCode 201: Bitwise And Of Numbers Range

def rangeBitwiseAnd(mut left: int, mut right: int) -> Result[int, Error]:
    try:
        i = 0
        while left != right:
            left = left >> 1
            right = right >> 1
            i += 1
        result: int = left << i
        return result
    except Error as error:
        raise error

def main():
    try:
        range_bitwise_and_result_1: int = rangeBitwiseAnd(5, 7)
        assert range_bitwise_and_result_1 == 4
        range_bitwise_and_result_2: int = rangeBitwiseAnd(0, 0)
        assert range_bitwise_and_result_2 == 0
        range_bitwise_and_result_3: int = rangeBitwiseAnd(1, 2147483647)
        assert range_bitwise_and_result_3 == 0
    except Error:
        assert False
