# LeetCode 39: Combination Sum

def combinationSum(candidates: list[int], target: int) -> Result[list[list[int]], Error]:
    try:
        res = []

        def dfs(i, cur, total) -> Result[None, Error]:
            try:
                if total == target:
                    res.append(cur.copy())
                    return
                if i >= len(candidates) or total > target:
                    return

                candidate = candidates[i]
                if candidate is None:
                    raise IndexError("candidate index out of bounds")
                cur.append(candidate)
                next_candidate = candidates[i]
                if next_candidate is None:
                    raise IndexError("candidate index out of bounds")
                contract_result_2: None = dfs(i, cur, total + next_candidate)
                cur.pop()
                contract_result_3: None = dfs(i + 1, cur, total)
                return None
            except Error as error:
                raise error

        contract_result_1: None = dfs(0, [], 0)
        return res
    except Error as error:
        raise error

def main():
    try:
        contract_result_4: list[list[int]] = combinationSum([2,3,6,7], 7)
        assert str(contract_result_4) == '[[2, 2, 3], [7]]'
    except Error:
        assert False
